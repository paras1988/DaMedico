package com.interviewbit.arrays.Bucketing_Sorting;


/**
 * You are given an array (zero indexed) of N non-negative integers, A0, A1 ,…, AN-1.
 * Find the minimum sub array Al, Al+1 ,…, Ar so if we sort(in ascending order) that sub array, then the whole array should get sorted.
 * If A is already sorted, output -1.
 *
 * ) If the input array is [10, 12, 20, 30, 25, 40, 32, 31, 35, 50, 60], your program should be able to find that the
 * subarray lies between the indexes 3 and 8.
 *
 * 2) If the input array is [0, 1, 15, 25, 6, 7, 30, 40, 50], your program should be able to find that the subarray
 * lies between the indexes 2 and 5.
 *
 */
public class MaximumUnsortedSubarray_doIT {
    public static void main(String[] args) {
        Integer[] A = {1, 2, 3, 4 };
    }
}
